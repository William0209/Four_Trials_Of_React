// import createContext
import { createContext } from "react";

// Create and export context called UserContext, initialise it with null
// syntax: export const variableName = ...
export const UserContext = createContext(null);
