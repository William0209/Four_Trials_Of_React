// import createContext
import { createContext } from "react";

// Create and export context called ThemeContext, initialise it with null
export const ThemeContext = createContext(null);
// syntax: export const variableName = ...
